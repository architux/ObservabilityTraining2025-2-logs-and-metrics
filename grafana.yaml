apiVersion: 1

datasources:
  - name: flask_loki
    type: loki
    access: proxy
    orgId: 1
    url: http://flask_loki:3100/
    basicAuth: false
    isDefault: true
    version: 1
    editable: false
  - name: flask_prometheus
    type: prometheus
    access: proxy
    url: http://flask_prometheus:9090/
